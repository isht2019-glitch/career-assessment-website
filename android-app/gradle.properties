# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
android.enableJetifier=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Gradle optimization settings
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+UseParallelGC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# Disable JDK image transformation to prevent jlink.exe errors with SDK 30
android.enableJdkImageTransformation=false
android.experimental.enableJdkImageTransformation=false
android.experimental.enableJdkImageTransform=false
android.experimental.enableJdkImageTransformForDebug=false
android.experimental.enableJdkImageTransformForRelease=false
android.experimental.disableJdkImageTransform=true
android.disableJdkImageTransform=true

# Disable problematic features for stability
org.gradle.unsafe.configuration-cache=false

# Let Android Studio use its bundled JDK (comment out if you have Java installed)
# org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-11.0.28.6-hotspot
